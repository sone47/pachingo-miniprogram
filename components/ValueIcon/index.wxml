<view class="priority">
  <t-icon name="{{iconName}}{{isFilled ? '-filled' : ''}}" size="{{ iconSize }}"></t-icon>
  <text class="priority-text{{ isValueInsideIcon ? ' inside' : '' }}">{{ value }}</text>
</view>
